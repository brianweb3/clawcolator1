# 🦾 Clawcolator: Agent-First Fork of Percolator

**Clawcolator** — это agent-first fork протокола Percolator, в котором все рыночные решения полностью делегированы автономному OpenClaw-агенту, а протокол выполняет роль строгого исполнительного и защитного слоя.

## 🎯 Ключевая идея

**Percolator исполняет правила. OpenClaw принимает решения. Clawcolator объединяет их в одну систему.**

### Архитектурное разделение

```
┌─────────────────────────────────────────────────────────┐
│              OpenClaw Agent (Decisions)                 │
│  • Trade acceptance/rejection                          │
│  • Liquidity management                                │
│  • Market parameters (leverage, spreads, funding)      │
│  • Matching logic                                       │
│  • Risk management                                      │
│  • Anomaly detection & response                        │
│  • Emergency shutdown                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│         Clawcolator Core (Enforcement)                  │
│  • Balance & PnL accounting                             │
│  • Margin & liquidation checks                          │
│  • Invariant verification                               │
│  • Can reject ANY agent decision                        │
│  • No direct fund access for agent                      │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│              Percolator Risk Engine                      │
│  • Formally verified accounting                         │
│  • Conservation guarantees                              │
│  • User isolation                                        │
└─────────────────────────────────────────────────────────┘
```

## 🧠 Роль OpenClaw-агента

OpenClaw-агент — единственный субъект принятия решений в системе.

### 1. Принятие/отклонение сделок
- Решает, брать ли позицию с другой стороны
- Может отказать в любой сделке
- Определяет условия исполнения (цена, размер)

### 2. Управление ликвидностью
- Определяет, сколько капитала активно
- Перераспределяет средства между аккаунтами
- Уходит в защитный режим при риске

### 3. Управление параметрами рынка
Агент динамически задаёт:
- **Плечо** (leverage limits)
- **Лимиты позиций** (position limits)
- **Спреды** (bid/ask spreads)
- **Funding rate** (funding rate per slot)

Все параметры проходят проверку ядром протокола.

### 4. Выполнение матчинга
- Агент выступает второй стороной сделки
- Определяет условия исполнения
- Может реквотить или отклонять

### 5. Управление риском
- Снижает экспозицию
- Хеджируется
- Закрывает позиции при угрозе

### 6. Реагирование на аномалии
- Волатильность
- Проблемы оракулов
- Манипуляции

Может:
- Заморозить рынок
- Снизить лимиты
- Остановить торговлю

### 7. Самостоятельное выключение рынка
OpenClaw может принять решение: "Рынок небезопасен" и инициировать controlled shutdown.

## 🔒 Роль протокола (что НЕ делегировано)

Clawcolator Core:
- ✅ Считает балансы и PnL
- ✅ Проверяет маржу и ликвидации
- ✅ Следит за инвариантами
- ✅ Может отклонить любое действие агента

**Агент не имеет прямого доступа к средствам.**

## 📐 Архитектура реализации

### OpenClawAgent Trait

```rust
pub trait OpenClawAgent {
    /// Решение о принятии/отклонении сделки
    fn decide_trade(
        &self,
        context: &AgentContext,
        trade_request: &TradeRequest,
    ) -> Result<TradeDecision>;
    
    /// Управление параметрами рынка
    fn get_market_params(
        &self,
        context: &AgentContext,
    ) -> Result<MarketParams>;
    
    /// Решение о ликвидности
    fn decide_liquidity_allocation(
        &self,
        context: &AgentContext,
    ) -> Result<LiquidityAllocation>;
    
    /// Решение о риске
    fn assess_risk(
        &self,
        context: &AgentContext,
    ) -> Result<RiskAssessment>;
    
    /// Решение об аномалиях
    fn detect_anomalies(
        &self,
        context: &AgentContext,
    ) -> Result<AnomalyResponse>;
    
    /// Решение о shutdown
    fn should_shutdown(
        &self,
        context: &AgentContext,
    ) -> Result<bool>;
}
```

### ClawcolatorEngine

Обертка над `RiskEngine`, которая:
1. Получает решения от агента
2. Проверяет их через протокол
3. Исполняет только валидные решения

### Интеграция с MatchingEngine

Существующий `MatchingEngine` trait заменяется на `OpenClawAgent::decide_trade()`, который возвращает `TradeExecution`.

## 🔄 Flow операций

### Trade Flow

```
User Request → ClawcolatorEngine::execute_trade()
    ↓
Agent.decide_trade() → TradeDecision
    ↓
Clawcolator validates decision
    ↓
RiskEngine.execute_trade() → Result
```

### Parameter Update Flow

```
Agent.get_market_params() → MarketParams
    ↓
Clawcolator validates params (bounds, safety)
    ↓
Update RiskParams (if valid)
```

### Risk Management Flow

```
Agent.assess_risk() → RiskAssessment
    ↓
Clawcolator checks if action needed
    ↓
Execute risk reduction (if approved)
```

## 🛡️ Безопасность

1. **Все решения агента проверяются протоколом**
2. **Агент не может обойти инварианты**
3. **Агент не имеет прямого доступа к средствам**
4. **Протокол может отклонить любое решение**

## 📝 Отличия от Percolator

| Аспект | Percolator | Clawcolator |
|--------|------------|-------------|
| Matching | Внешний MatchingEngine | OpenClaw Agent |
| Parameters | Статические RiskParams | Динамические от агента |
| Risk Management | Автоматический | Агент + протокол |
| Liquidity | LP управляют | Агент управляет |
| Shutdown | Нет | Агент может инициировать |

## 🚀 Реализация

1. ✅ Создать `OpenClawAgent` trait
2. ✅ Создать `ClawcolatorEngine` wrapper
3. ✅ Интегрировать с существующим `RiskEngine`
4. ✅ Реализовать валидацию решений агента
5. ✅ Добавить динамическое управление параметрами
6. ✅ Реализовать anomaly detection hooks
7. ✅ Добавить shutdown механизм
8. ✅ Создать пример реализации агента

## 📖 Использование

### Базовый пример

```rust
use percolator::clawcolator::*;
use percolator::{RiskParams, U128};

// Создать агента
let agent = SimpleClawAgent::new(
    1_000_000,  // max_position_size
    1000,       // max_leverage_bps (10x)
    10,         // spread_bps (0.1%)
);

// Создать движок
let base_params = RiskParams { /* ... */ };
let mut engine = ClawcolatorEngine::new(base_params);

// Выполнить сделку через агента
engine.execute_trade(
    &agent,
    user_idx,
    oracle_price,
    size,
    now_slot,
)?;
```

### См. примеры

Полный пример реализации агента: `examples/simple_claw_agent.rs`

## 🔧 Особенности реализации

- **no_std совместимость**: Clawcolator работает в `no_std` окружении
- **Feature gate**: Используйте `--features clawcolator` для включения
- **Безопасность**: Все решения агента валидируются протоколом
- **Расширяемость**: Легко создать собственного агента, реализовав `OpenClawAgent`
